# 프로젝트 달마루 Codex
- 한국어 존댓말로 짧게 보고한다. 기존 코드·패턴과 사용자 변경을 존중하고 관련 없는 변경을 섞지 않는다.
- 이 폴더는 아직 구현 파일이 없는 Git 저장소다. 설계 탐색은 docs/README.md, 현재 상태는 PROGRESS.md를 필요할 때 확인한다. 구현 완료로 추정하지 않는다.
- 의도한 동작은 담당 설계, 실제 동작은 코드·시험이 기준이다. 충돌 위치·영향을 확인한다.
- 요청 → Router → 관련 문서 1~3개의 필요한 절 → 관련 코드 순으로 탐색한다. 전체 저장소·과거 기록을 일괄 로드하지 않는다.
- 파일/심볼 검색부터 시작하고 독립 조회는 묶는다. 변경 없는 재읽기·중복 분석을 피하고 근거가 충분하면 멈춘다.
- 서버 판정·소유권·중복 처리 방지를 유지한다. 상세 아키텍처는 관련 설계에서 확인한다.
- 작은 작업은 직접 처리하고 큰 작업만 짧은 Active Spec을 사용한다. 웹·위임은 필요한 경우만 사용한다.
- 위험에 맞는 최소 검증부터 실행한다. 새 변경·실패·위험 없이 성공한 빌드·테스트를 반복하거나 범위를 확대하지 않는다.
- 수정 후 PROGRESS.md, 테스트 실행 시 TEST_RESULT.md를 간결하게 갱신한다. 변경·검증·미해결 사항만 보고하고 코드·문서 전문을 재출력하지 않는다.
- Git 작업·배포는 명시 요청 시만 수행한다. 작업 전후 변경 범위를 확인한다.
